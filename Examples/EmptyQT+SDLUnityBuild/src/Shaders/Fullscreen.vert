////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

in vec2  aPosition;
out vec2 vScreenPos;

void main(void) {
	vScreenPos = (aPosition * 0.5) + vec2(0.5);
	gl_Position = vec4(aPosition.x, aPosition.y, 0.0, 1.0);
}