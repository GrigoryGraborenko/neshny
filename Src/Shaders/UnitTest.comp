////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
void main(void) {

	vec3 global_id = gl_GlobalInvocationID;
	int item_index = int(global_id.x) + (int(global_id.y) + int(global_id.z) * 32) * 32 + uOffset;

	if (item_index >= uCount) return;

#ifdef TEST_RANDOM
	RandomBuffer.i[item_index] = Random();
#endif
}